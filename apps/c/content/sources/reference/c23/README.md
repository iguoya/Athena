WG14 公开工作稿 N3220（2024-02-22）与 C23 草案 N3219 几乎只差 Annex K
一条脚注，用来离线核对条款号。正式标准是 ISO/IEC 9899:2024，需向 ISO 购买，
不进本仓库。

```sh
cd apps/c && python3 scripts/fetch-sources.py
```

会落下 `n3220.pdf`（约 4 MB）。该 PDF **不进 git**。不要用更新的 C2y 稿
（如 N3886）替换它来当现行语义。
