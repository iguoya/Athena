Steve Summit 的 [C FAQ](https://c-faq.com/) 允许个人查阅，禁止再发布。
本目录的 HTML **不进 git**。需要离线对照时：

```sh
cd apps/c && ./scripts/fetch-sources.sh
```

会落下 `ptrs.html`、`aryptr.html`、`malloc.html`。写题只引用题号，不整页复制进教案。
