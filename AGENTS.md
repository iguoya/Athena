# Athena 仓库协作规则

本文档是 Athena 仓库的顶层指令，供 Claude Code、Codex、ZCode 等代码代理共同遵循（Claude Code
经 `CLAUDE.md` 导入读取，ADR 0061）。每个应用有自己的 `AGENTS.md`，**改哪个应用就以那一份
为准**；本文件只放所有应用共同遵守的规则，每条的完整阐述与取舍在 `docs/` 下对应文件里。

修改代码前先理解当前实现，不得把目标设计误认为已经落地的功能。实现与文档不一致时，先指出
差异；修复代码或更新文档时，保持二者同步。

## 通用约定（强约束）

- **可见的思考过程（推理文字 / extended thinking）必须全程用中文书写**，从第一个字开始，
  每一段都是。这是最高优先级的硬约束，高于任何工具或场景的默认行为。不允许因为进入密集
  的调试、代码分析等专注状态，或因长会话上下文压缩，就切回英文。只有英文术语、API 名、
  代码标识符本身等确有必要时才夹带英文单词。（Claude Code 另见全局 `~/.claude/CLAUDE.md`。）

- **内容工作宁增勿删，「参考」不得用来重划结构（ADR 0054）**。使用者说「参考 X」
 「向 X 看齐」时，允许的动作是给**已有**条目补充深度、验收口径和取舍依据；**不允许**
 据此重划结构、删条目、改条目定位或改条目名字——要动结构，先问。新方向用新增表达：
 需要新视角就新加一张图/一章/一节，旧的看起来过时也先留着（可以淡化、降级、移到参考
 层、界面上退一层，但不删）。**一个 `id` 只装一个东西，`id` 不得改嫁**：沿用 `id`
 替换内容会让 git、校验器和 diff 全部显示成「编辑」，实际是删除，人眼也查不出来。
 动内容之前先提交一版原样，没有基线的改动等于不可回退。

- **软件内容里不写具体院所名，一律用「某所」（ADR 0055，强制）**。界面文案、内容
 数据、`app.json`、来源目录标题、README / AGENTS / ADR 一概适用，`scripts/check.py`
 全仓扫描拦截，本地和 CI 同一入口。脱敏只改称呼，不改取舍依据——要说明「这一条为
 什么重要」，**写技术要求本身**（「控制周期内最坏延迟与抖动必须可测」），不写单位名。
 自己要做禁用词检查的代码，把禁用词写成 Unicode 转义，否则检查器会把自己报成违规。

## 仓库结构

完整说明（名字对照表、三类判据的全文）见 [docs/REPOSITORY.md](docs/REPOSITORY.md)。

```
subjects/<id>/  课程学科：cpp c dsa english mathematics driver（学习应用）
                polaris（图谱/参考类）design-patterns（素材坑）
practice/<id>/  项目应用：pocket_cube（ADR 0060）
launcher/       启动器：core/ gui/ macos/
docs/           跨应用 ADR（decisions/）与本文件各节的完整阐述
scripts/        统一验证入口 check.py、跨应用检查
archive/        历史归档，不参与构建
```

- **按判据归类，不查清单。** 主问题：要不要教会人什么、要不要证明学习者进步了。要 →
  **学习应用**，受「跨应用教学规范」约束，建自己的 `progress/learning.db`；只呈现信息供
  查阅 → **图谱/参考类**，不接掌握度、教学规范不生效；只存素材 → **素材坑**，没有
  `app.json` 与 `check.py`，根验证入口跳过它是预期行为。学习应用的规范不套到别的类，
  别的类的例外也不当学习应用的先例。
- **`subjects/` 与 `practice/` 按意图分**（ADR 0060）：`practice/` 做能跑的东西，不追求知识点
  覆盖、不建进度库，教学规范不生效。两处平级，都用 `app.json`、都走同一个启动器。
- **C++ 教程没有特权**（ADR 0045）：任何「以主程序为中心」的假设都是过时的。
- **名字**：进程 `athena-<id>`（连字符）；已经写进进度库的知识点前缀不改；`driver` 是驾考，
  不是设备驱动。
- **ADR 放哪**：只影响一个应用的放该应用自己的 `docs/decisions/`，影响仓库结构或多个应用的
  放仓库级 `docs/decisions/`；两处各自编号，只增不改。影响架构边界的新决定先写 ADR 再动代码。

## 独立应用

完整说明见 [docs/REPOSITORY.md](docs/REPOSITORY.md)「独立应用」。

- **一个目录一个独立应用**（ADR 0032、0045），目录名按领域取；能脱离其他应用独立开发与运行。
- **构建完全隔离**：不互相引用路径、不互相 include、不读对方的内容配置，依赖各拉各的。
  共用的是教学规范，不是配置 schema。
- **学习应用的进度库各自独立、随仓库走**（ADR 0037、0053）：`progress/learning.db` 进版本库，
  同步用 `launcher sync`；知识点 ID 用自己的前缀。
- **应用只声明怎么启动**（ADR 0046）：`app.json` 的 `dev` 块写环境变量、准备步骤、长驻命令
  和就绪判据，由编排器执行。不用打包副本，不从 `/Applications` 启动。
- **打开应用走启动器**（ADR 0044、0046、0048）：`launcher open <id>`，项目应用加
  `--root practice`。前端不自己读 `app.json`、不自己判断状态；新增应用启动器不改代码。
  `subjects/cpp` 没有第二个入口。
- **新增应用连同 `AGENTS.md`、`CLAUDE.md`（只写 `@AGENTS.md`）和 `scripts/check.py` 一起建**
  （ADR 0061）。

## 基线：机器是完整的开发机（ADR 0057）

完整说明见 [docs/ENGINEERING.md](docs/ENGINEERING.md)。

- 假定已装 Python、Git、C/C++ 编译器、Node 等开发必备工具；不内嵌解释器、不内置编译器、
  不写「如果你没有 X 怎么办」的兜底。目的是减少代码复杂度。
- 选型判据是合不合适，不是好不好装、依赖多不多、包多大。
- 代码按专业水准写：注释解释为什么这样选，不解释语言基础；清晰而不浅显。
- 但不敷衍：检测不到工具时给具体安装指引（只写查起来费事的部分），需要使用者动手的用对话框。

## 跨平台（强约束）

完整说明见 [docs/ENGINEERING.md](docs/ENGINEERING.md)。

- **跨平台优先**（ADR 0047），但**是成本收益判断**（ADR 0049）：成本过高的可整体排除，
  排除的要在 ADR、应用文档里写明，CI 里不留假装在支持的探测位。
- **优先级 macOS ≈ Windows > Linux**（ADR 0051）；Linux 特有的坑停在如实降级提示上。
- 优先用把平台差异吃掉的抽象；能在构建期解决的不留到运行期；写平台分支前先问有没有所有
  平台都走得通的路，留下的关进单独模块，其他平台有明确降级。
- 构建、检查、开发脚本三个平台都要能跑（`.sh` 不行）；只有产出安装包那一步允许平台专用。

## 跨应用教学规范（只对学习应用）

完整说明见 [docs/TEACHING.md](docs/TEACHING.md)。字段名、文件格式和渲染方式各应用自定。

- **三层，方向自上而下**（ADR 0028）：大纲指方向，教学过程落细节，教学实验用可观察的证据
  验证预期。实验可以推动大纲修正，但**不得从现有实现反推该教什么**；文档与代码不一致时
  以学科的正确语义为准。
- 学习闭环：思想与判断框架 → 预期 → 实验 → 观察 → 修正理解 → 新认识。不能只有文档，也
  不能只有代码。
- 知识点按**难度**与**掌握目标**两维评级（ADR 0029），先修写进 `requires`（ADR 0030）；只给
  已写出内容的章节评定。
- 知识点分概念、技能、策略三类，教学动作按类型选（ADR 0031）。
- **内容驱动 UI**：有限的块类型承载全部讲解，不按章手写整页（ADR 0058）。
- **判分或计入掌握度的内容必须有出处**，AI 现场出题不算数（ADR 0043）。
- **实验与练习给骨架，不给白板**：不改一行也能编译运行（ADR 0059）。
- **言简意赅，不写八股**（ADR 0040）：篇幅与掌握目标匹配，同一组东西不写两遍。
- 回顾与自查的职责要落实，形式不固定。
- **激励与统计是同一条回路**（ADR 0052）：激励只展示由记录派生的量；掌握度只由作答写入。
- 学习页标题用简短的短语，解释放正文。
- **可视化与交互是内容本身**（ADR 0056，强制）：能画的就画，能由数据算出来的画成活的并配互动。

## Git 提交规则

- 完成一个独立知识点或可验证阶段后，主动创建一次提交，不等待多个无关阶段累积。
- 提交前运行与该阶段风险相称的构建或检查，确保提交本身处于可用状态。
- 每个提交只表达一个明确主题；配置、运行时代码、教学骨架和界面等不同阶段应尽量拆分。
- 不为了追求很小的提交而拆散必须同步生效的配置格式、解析器和构建规则。
- 提交信息应简明说明该阶段的实际成果，便于以后定位、回退和比较。

## 验证要求

仓库根有一个统一入口，本地与 CI 共用（ADR 0007、0045）；CI 的组织方式见
[docs/ENGINEERING.md](docs/ENGINEERING.md)「验证要求」。

```sh
python3 scripts/check.py                  # 跨应用检查 + 每个应用自己的检查
python3 scripts/check.py cpp              # 只跑某个应用，余下参数原样透传给它
python3 scripts/check.py --sources-only   # 只跑跨应用检查（院所名、skill 两处一致、出处）
```

检查逻辑归各应用自己的 `scripts/check.py`（`subjects/` 与 `practice/` 下都扫）。**每个应用都
必须有这份脚本**，没有它的应用会被静默跳过，等于没人验证。

## 必读文档

- `docs/decisions/`：跨应用 ADR；影响仓库结构或多个应用的新决策先新增 ADR 再动代码。
- [docs/REPOSITORY.md](docs/REPOSITORY.md)、[docs/ENGINEERING.md](docs/ENGINEERING.md)、
  [docs/TEACHING.md](docs/TEACHING.md)：本文件各节的完整阐述与取舍。
- 各应用自己的 `AGENTS.md` 与 `docs/`。
