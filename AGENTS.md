# Athena 仓库协作规则

本文档是 Athena 仓库的顶层指令，供 ZCode、Codex、Claude Code 等代码代理共同遵循
（Claude Code 经 `CLAUDE.md` 导入读取）。

仓库里每个学习应用都有自己的 `AGENTS.md`，**改哪个应用就以那一份为准**；本文件只放
所有应用共同遵守的东西。修改代码前先理解当前实现，不得把目标设计误认为已经落地的功能。

## 通用约定（强约束）

- **可见的思考过程（推理文字 / extended thinking）必须全程用中文书写**，从第一个字开始，
  每一段都是。这是最高优先级的硬约束，高于任何工具或场景的默认行为。不允许因为进入密集
  的调试、代码分析等专注状态，或因长会话上下文压缩，就切回英文。只有英文术语、API 名、
  代码标识符本身等确有必要时才夹带英文单词。（Claude Code 另见全局 `~/.claude/CLAUDE.md`。）

## 仓库结构

```
apps/<id>/     一个目录一个独立学习应用，彼此完全平级
  cpp/         C++ 教程（GTK4 / gtkmm，原来的"主程序"）
  c/           C 语言（Qt Quick / QML）
  dsa/         数据结构与算法（Tauri）
  english/     英语学习（Tauri）
  mathematics/ 数学学习（Tauri）
  driving/     驾驶学习（Flutter 桌面，科目一 / 科目四）
launcher/      启动器：macos/（Swift 菜单栏常驻）、core/、gui/
docs/decisions/  跨应用的架构决策记录（ADR）
scripts/       仓库级脚本：统一验证入口 check.py、跨应用内容出处检查
archive/       历史归档，不参与构建
```

**C++ 教程没有特权**（ADR 0045）：它和别的学科一样住在 `apps/` 下，仓库根不再有它的
源码、构建文件、脚本和文档。任何"以主程序为中心"的假设都是过时的。

只影响单个应用的 ADR 放在**该应用自己的** `docs/decisions/` 下；影响仓库结构或多个
应用的才放仓库级 `docs/decisions/`。两处各自延续编号，历史编号不重排，所以两边都有跳号。

## 独立应用（`apps/`）

- **一个目录一个独立应用**（ADR 0032、0045），自带构建系统、依赖、界面技术和内容体系。
  目录名按领域取（`cpp`、`c`、`dsa` 这样的领域名），不带实现技术名——手段会换，领域不会。
- **各应用有自己的 `AGENTS.md`**：改该应用时以它为准，不把别的应用的规则套进去。
  每个应用必须能**脱离其他应用独立开发与运行**。
- **构建完全隔离**：应用之间不互相引用路径、不互相 include 头文件、不读对方的内容配置，
  第三方依赖各拉各的。共用的是教学**规范**（下一节），不是配置 schema——复用规范零成本，
  复用 schema 会立刻产生耦合。
- **学习库各自独立**（ADR 0037）：每个应用自建、自管、自迁移它自己的进度库，放在应用
  自己的用户数据目录下，不共用别人的库，也不假设别人跑过。知识点 ID 用自己的前缀
  （C 语言 `c.`，数据结构 `dsa.`）。
- **应用只声明怎么启动，不写启动脚本**（ADR 0046）：`app.json` 的 `dev` 块写清
  环境变量、准备步骤（装依赖 / 增量构建）、长驻命令和就绪判据，执行统一由
  `launcher/core` 的编排器负责。热更新，改源码即时可见；不要用打包 `.app` / DMG，
  也不要从 `/Applications` 启动——打包副本会让人不知不觉对着旧版本工作。
- **打开哪个应用走 `launcher/` 的启动器**（ADR 0044 / 0046 / 0048）：跨平台版托盘
  常驻，macOS 版在菜单栏（平台专属，理由与降级见 ADR 0048），终端用
  `athena-dev open <id>`。三个前端同一条执行路径——都不自己读 `app.json`、不自己
  判断状态，一律向编排器要（`athena-dev list --json`）。新增应用照样放一份带
  `dev` 声明的 `app.json` 即可，启动器不需要改代码。
- `apps/cpp` 首页的学科路线图仍可点开别的应用（ADR 0032 第 5 条），但它只是入口之一，
  不再是必经之路。

## 基线：使用者是资深开发人员，机器是一台完整的开发机

**运行前提**：可以直接假定机器上装有 Python、Git、C/C++ 编译器、Node 这类开发
必备工具，不要为「万一没装」付出代价——不内嵌解释器（例如拿 Pyodide 替代本机
Python + SymPy）、不内置编译器、不把功能降级成「没有工具链也能凑合用」、不在
文档和代码里堆「如果你没有 X 怎么办」的兜底分支。

**技术选型**：**判据是这个技术合不合适，不是它好不好装、依赖多不多、包体积多大。**
运行库的安装与分发成本在本仓库不计入权衡；该用重依赖就用（SymPy 即是一例），
不要为了「零依赖」「好分发」「免安装」退而求其次。

**但这不等于可以敷衍。** 检测不到某个工具时仍要给出具体的安装指引——装什么、
哪个组件、什么命令，而不是一句「请先安装」。前者是尊重使用者的时间，后者才是
为不存在的用户妥协。`apps/dsa` 找不到 C++ 编译器时按平台给出的三选一
（VS / LLVM / MSYS2，各带组件名与命令）就是这个标准。

## 跨平台优先（强约束）

**技术选型和代码编写一律跨平台优先（ADR 0047）**，适用于仓库里每一个应用。
但**跨平台是成本收益判断，不是"三平台必须对称"（ADR 0049）**：某个软件、工具
甚至整个项目，跨平台成本很高时就整体排除在跨平台之外，让它在专用平台上发挥最大
优势——排除的粒度可以很大，不限于某个函数里的平台分支。被排除的必须写明白：
ADR 里写清成本、收益与降级，应用文档里写明哪个平台不支持，CI 里不留假装在支持
的探测位。含糊比排除更糟。当前的排除清单在 ADR 0049 里。

**平台之间有优先级：macOS ≈ Windows > Linux（ADR 0051）。** 不是放弃 Linux，
是排序——遇到取舍先保前两个。选型时 macOS 与 Windows 上的成熟度是硬指标，Linux
上的表现是加分项；在 Linux 上是一等公民、却要在 Windows 上跟上游缺陷缠斗的框架，
在这个排序下是减分的。同时冒出来的平台问题，先修 Windows 与 macOS 的；Linux 特有
的坑（Wayland 不允许抢焦点、GNOME 默认没有托盘区）停在如实降级提示上就够。

没被排除的地方，下面这些照旧：

- **选型时先确认 macOS 与 Windows 可用**，再看 Linux；只在部分平台可用的，
  要么找等价替代，要么在 ADR 里写明为什么值得、其他平台怎么降级。
- **优先选把平台差异自己吃掉的抽象**，而不是自己按平台分叉：打开 URI 用系统默认
  处理器，问桌面能力用框架的设置项，界面用跨平台工具包。这类抽象往往还更正确——
  按平台判断会漏掉同一平台上的例外。
- **能在构建期解决的不要留到运行期**：应用图标、包元数据、依赖收集都是打包工作，
  不该变成代码里的分支。
- **构建过程本身也跨平台**：构建系统、检查入口、开发脚本都要能在三个平台上跑
  （Meson / CMake / Cargo / npm / Python 都行，`.sh` 不行——Windows 要靠 Git Bash
  或 WSL）。只有产出安装包那一步允许平台专用，因为 `.dmg` / `.deb` / `.msi` 本就
  不同；验证、生成、启动这些每天都跑的环节不允许。支持哪个平台就在 CI 里跑哪个，
  否则支持会悄悄退化。
- **写平台分支前先问**：这条分支服务什么业务？有没有所有平台都走得通的路？如果有，
  平台特有的那条还剩多少价值？留下来的必须关在单独模块里，其他平台有明确降级行为，
  而不是编不过或崩溃。

## 跨应用教学规范

这些是所有学习应用共同的方法论；**字段名、文件格式和渲染方式各应用自定**。

- **学习内容分三层，方向自上而下（ADR 0028）**：**教学大纲**一章一份，只指引大方向
  ——痛点与来历、心智模型、讲什么与边界、判断与代价、落点；**教学过程**在大纲划定的
  方向内落实细节与教学目的；**教学实验**用可运行、结果可观察的证据验证或推翻预期。
  三层可以重复表达同一条规则，但语义必须一致；实验反馈可以上溯推动大纲修正，
  但不得从现有实现反推该教什么。
- **教学方向由知识点和教学大纲主导**，代码负责把思想转化为短小、真实、可运行且结果
  可观察的行动。不能只有文档：被动阅读容易失去注意力，也难以学有所用；不能只有代码：
  容易失去全局知识框架，陷入实现细节。统一学习闭环是"建立思想与判断框架 → 形成预期 →
  编写或运行实验 → 观察结果 → 对照并修正理解 → 形成更深入的新认识"。
- **不得从现有实现反向决定该教什么**。出现文档与代码不一致时，先依据该学科的正确语义
  核对文档，再让代码覆盖经确认的学习目标，不得为了迁就当前实现而删减或扭曲知识点。
- **知识点按「难度」和「掌握目标」两个维度分别评级**（ADR 0029），先修关系写进
  `requires`（ADR 0030）。难度、掌握目标、知识类型和先修**只给已经写出教学内容的
  章节评定**，其余留空，不提前判断。
- **知识点分概念、技能、策略三类，教学动作按类型选**（ADR 0031）：概念要正例反例并排，
  技能要示范加变式练习，策略要在情境里选择并说明依据；不要三类都套同一个
  「讲解 + 一道选择题」模板。
- **要言之有物、言简意赅，不写八股文**（ADR 0040）。固定范式、固定标题、每节配齐同一套
  组件——这些是参考，不是模具。**篇幅要与掌握目标匹配**：难度低、只要求「掌握」的知识点，
  不该比难度高、要求「精通」的写得还长；写完对照该应用的难度与掌握目标校准一遍，
  篇幅倒挂就是该删的信号。**同一组东西不要写两遍**：能合并的节不要拆，大纲只指方向，
  必要的细节一律下放到对应的子章节。
- **回顾与自查的职责要落实，但形式不固定——拒绝为凑格式而写**。读者学完一节，要有地方
  帮他回头确认自己掌握了什么：可以是小结，也可以是随堂考核、误区清单，或这一节本来就有
  的收尾段落。收束与随堂考核不是互斥关系，可以并存也可以只有其一。判断标准是"有没有
  地方让读者回头确认"，不是"有没有一个叫「小结」的标题"。
- **内容必须有出处**（ADR 0043）：例题、习题和诊断题要结合真实教材，不要轻易自造；
  各应用用 `content-contract.json` 声明自己的字段名，由 `scripts/check-app-sources.mjs`
  统一校验。
- **学习页的标题用简短的短语式标题**；解释性或描述性的完整句子放在标题后的正文中，
  不通过扩宽栏宽或缩小字号适配冗长标题。
- **优先图文结合**，能用图说明的不要只堆文字：概念结构、状态迁移、执行流程、分类对比
  优先画成图或表。能由运行时数据算出来的结构就画成活的并配上互动，不要做成会和数据
  漂移的图片；纯概念示意用静态图。加视觉元素前先问"一句话能不能说得同样清楚"，
  把正文清单排成方框不算可视化。

## Git 提交规则

- 完成一个独立知识点或可验证阶段后，主动创建一次提交，不等待多个无关阶段累积。
- 提交前运行与该阶段风险相称的构建或检查，确保提交本身处于可用状态。
- 每个提交只表达一个明确主题；配置、运行时代码、教学骨架和界面等不同阶段应尽量拆分。
- 不为了追求很小的提交而拆散必须同步生效的配置格式、解析器和构建规则。
- 提交信息应简明说明该阶段的实际成果，便于以后定位、回退和比较。

## 验证要求

仓库根有一个统一入口，本地与 CI 共用（ADR 0007、0045）：

```sh
python3 scripts/check.py            # 跨应用内容出处检查 + 每个应用自己的检查
python3 scripts/check.py cpp        # 只跑某个应用，余下参数原样透传给它
python3 scripts/check.py --sources-only   # 只跑跨应用的内容出处检查
```

检查逻辑归各应用自己（`apps/<id>/scripts/check.py`），根入口只负责依次调用，
新增应用放一份自己的 `check.py` 就会被带上。**每个应用都必须有这份脚本**——
没有它的应用会被根入口静默跳过，等于没人验证。

CI 跑的是同一条入口，每个应用一个 job：依赖各装各的，命令都是
`python3 scripts/check.py <id>`。三个 Tauri 应用共用一个矩阵，新增同类应用
时往 `matrix.app` 里加一行；换一种技术栈就新起一组 job，因为要装的依赖不同。
支持哪个平台就在那个平台上跑，否则支持会悄悄退化（ADR 0047）。

## 必读文档

- `docs/decisions/`：跨应用 ADR，保存重要取舍的背景、决策与后果；影响仓库结构或
  多个应用的新决策应先新增 ADR 再动代码。
- `apps/<id>/AGENTS.md` 与 `apps/<id>/docs/`：该应用自己的规则与文档。

若实现与文档不一致，先指出差异；修复代码或更新文档时，保持二者同步。
